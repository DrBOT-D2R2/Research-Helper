# Shared

Reserved for cross-runtime contracts that need to be consumed by both frontend and backend. The MVP keeps schema duplication small, but this folder is the natural location for generated OpenAPI clients, shared JSON fixtures, or normalized vocabulary files during PS-1 expansion.

